--- # LNP API definition file; autogenerated from c-lightning CSV API file

types:
  - &u8 { snake: u8, pascal: U8, camel: u8 }
  - &node_id { snake: node_id, pascal: NodeId, camel: nodeId }
  - &wirestring { snake: wirestring, pascal: Wirestring, camel: wirestring }
  - &u16 { snake: u16, pascal: U16, camel: u16 }
  - &bool { snake: bool, pascal: Bool, camel: bool }

messages:
  0xFFF0: &status_log
    - { name: level, type: *u8, enum: log_level }
    - { name: peer, type: *node_id, optional: true }
    - { name: entry, type: *wirestring }
  0xFFF1: &status_io
    - { name: iodir, type: *u8, enum: log_level }
    - { name: peer, type: *node_id, optional: true }
    - { name: who, type: *wirestring }
    - { name: len, type: *u16 }
    - { name: data, type: *u8, len_var: len }
  0xFFF2: &status_fail
    - { name: failreason, type: *u8, enum: status_failreason }
    - { name: desc, type: *wirestring }
  0xFFF3: &status_peer_connection_lost
  0xFFF5: &status_peer_billboard
    - { name: perm, type: *bool }
    - { name: happenings, type: *wirestring }
...
