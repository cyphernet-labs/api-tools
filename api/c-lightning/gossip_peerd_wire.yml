--- # LNP API definition file; autogenerated from c-lightning CSV API file

types:
  - &short_channel_id { snake: short_channel_id, pascal: ShortChannelId, camel: shortChannelId }
  - &u16 { snake: u16, pascal: U16, camel: u16 }
  - &u8 { snake: u8, pascal: U8, camel: u8 }
  - &node_id { snake: node_id, pascal: NodeId, camel: nodeId }
  - &amount_sat { snake: amount_sat, pascal: AmountSat, camel: amountSat }
  - &bool { snake: bool, pascal: Bool, camel: bool }
  - &amount_msat { snake: amount_msat, pascal: AmountMsat, camel: amountMsat }
  - &u32 { snake: u32, pascal: U32, camel: u32 }
  - &u64 { snake: u64, pascal: U64, camel: u64 }

messages:
  3501: &gossipd_get_update
    - { name: short_channel_id, type: *short_channel_id }
  3601: &gossipd_get_update_reply
    - { name: len, type: *u16 }
    - { name: update, type: *u8, len_var: len }
  3503: &gossipd_local_add_channel
    - { name: short_channel_id, type: *short_channel_id }
    - { name: remote_node_id, type: *node_id }
    - { name: satoshis, type: *amount_sat }
    - { name: flen, type: *u16 }
    - { name: features, type: *u8, len_var: flen }
  3504: &gossipd_local_channel_update
    - { name: short_channel_id, type: *short_channel_id }
    - { name: disable, type: *bool }
    - { name: cltv_expiry_delta, type: *u16 }
    - { name: htlc_minimum_msat, type: *amount_msat }
    - { name: fee_base_msat, type: *u32 }
    - { name: fee_proportional_millionths, type: *u32 }
    - { name: htlc_maximum_msat, type: *amount_msat }
  3505: &gossipd_new_store_fd
    - { name: offset_shorter, type: *u64 }
  3506: &gossipd_local_channel_announcement
    - { name: len, type: *u16 }
    - { name: cannount, type: *u8, len_var: len }
...
